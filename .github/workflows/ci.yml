name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Run a one-line script
      - name: Running tests
        run: ./vendor/bin/phpunit tests/Feature/ApiTest.php --testdox

